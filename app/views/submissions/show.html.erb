<% content_for :title, @submission.title %>

<%= render "shared/developer_nav" %>

<main class="max-w-2xl mx-auto px-6 py-10">
  <div class="mb-8">
    <%= link_to "← Submissions", project_submissions_path(@project), class: "text-xs text-gray-400 hover:text-gray-700 transition-colors" %>
  </div>

  <div class="bg-white border border-gray-200 rounded-xl p-6 mb-5">
    <div class="flex items-start justify-between gap-4 mb-5">
      <h1 class="text-base font-semibold text-gray-900 leading-snug"><%= @submission.title %></h1>
      <span class="shrink-0 text-[10px] font-medium px-2 py-0.5 rounded-full border
        <%= @submission.status == 'accepted'  ? 'bg-blue-50 text-blue-700 border-blue-200' :
            @submission.status == 'shipped'   ? 'bg-green-50 text-green-700 border-green-200' :
            @submission.status == 'dismissed' ? 'bg-gray-50 text-gray-500 border-gray-200' :
            'bg-amber-50 text-amber-700 border-amber-200' %>">
        <%= @submission.status %>
      </span>
    </div>

    <p class="text-sm text-gray-700 leading-relaxed whitespace-pre-wrap"><%= @submission.body %></p>

    <div class="mt-6 pt-5 border-t border-gray-100 flex items-center gap-4 text-xs text-gray-400">
      <span class="font-medium text-gray-500"><%= @submission.collaborator.name %></span>
      <span><%= @submission.collaborator.email %></span>
      <span><%= @submission.created_at.strftime("%b %-d, %Y") %></span>
    </div>

    <% if @submission.github_issue_url.present? %>
      <div class="mt-4 pt-4 border-t border-gray-100">
        <%= link_to "GitHub issue ##{@submission.github_issue_number}", @submission.github_issue_url,
              target: "_blank", rel: "noopener noreferrer",
              class: "text-xs text-indigo-600 hover:underline font-medium" %>
      </div>
    <% end %>
  </div>

  <% if @submission.status == "pending" %>
    <div class="flex items-center gap-3">
      <%= button_to "Accept", accept_project_submission_path(@project, @submission), method: :post,
            class: "px-5 py-2.5 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 active:scale-[0.98] transition-all" %>
      <%= button_to "Dismiss", dismiss_project_submission_path(@project, @submission), method: :post,
            class: "px-5 py-2.5 border border-gray-200 text-sm text-gray-500 font-medium rounded-lg hover:border-red-200 hover:text-red-600 active:scale-[0.98] transition-all" %>
    </div>
  <% end %>

  <% if @submission.status == "accepted" %>
    <div class="flex items-center gap-3">
      <%= button_to "Mark as shipped", ship_project_submission_path(@project, @submission), method: :post,
            class: "px-5 py-2.5 bg-green-600 text-white text-sm font-medium rounded-lg hover:bg-green-700 active:scale-[0.98] transition-all" %>
    </div>
  <% end %>
</main>
