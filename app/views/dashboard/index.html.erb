<% content_for :title, "Inbox" %>

<%= render "shared/developer_nav" %>

<main class="max-w-4xl mx-auto px-6 py-10">
  <div class="mb-8">
    <h1 class="text-xl font-semibold text-gray-900">Triage inbox</h1>
    <p class="text-sm text-gray-400 mt-0.5">Review pending submissions from your collaborators.</p>
  </div>

  <% if @pending_submissions.any? %>
    <ul class="divide-y divide-gray-100 border border-gray-200 rounded-xl overflow-hidden bg-white">
      <% @pending_submissions.each do |submission| %>
        <li>
          <%= link_to project_submission_path(submission.project, submission), class: "flex items-start justify-between gap-4 px-5 py-4 hover:bg-gray-50 transition-colors" do %>
            <div class="min-w-0">
              <p class="text-sm font-medium text-gray-900 truncate"><%= submission.title %></p>
              <p class="text-xs text-gray-400 mt-1">
                <span class="font-medium text-gray-500"><%= submission.project.name %></span>
                &middot; <%= submission.collaborator.name %>
                &middot; <%= submission.created_at.strftime("%b %-d") %>
              </p>
            </div>
            <span class="shrink-0 mt-0.5 text-[10px] font-medium bg-amber-50 text-amber-700 border border-amber-200 px-2 py-0.5 rounded-full">pending</span>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <div class="border border-gray-200 rounded-xl bg-white px-6 py-16 text-center">
      <p class="text-sm font-medium text-gray-900">Inbox zero</p>
      <p class="text-sm text-gray-400 mt-1">No pending submissions to review.</p>
    </div>
  <% end %>
</main>
