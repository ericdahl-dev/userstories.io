<% if (website_id = ENV["UMAMI_WEBSITE_ID"].presence) && (script_url = ENV["UMAMI_SCRIPT_URL"].presence) %>
  <script defer src="<%= script_url %>" data-website-id="<%= website_id %>"></script>
<% end %>
